version: '3.8'

services:
  test-multi-route:
    image: nginx:alpine
    container_name: test-multi-route
    ports:
      - "9006:80"
    networks:
      - platform_network

networks:
  platform_network:
    external: true
    name: platform_network
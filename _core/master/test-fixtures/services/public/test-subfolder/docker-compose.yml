version: '3.8'

services:
  test-subfolder:
    image: nginx:alpine
    container_name: test-subfolder
    ports:
      - "9004:80"
    networks:
      - platform_network

networks:
  platform_network:
    external: true
    name: platform_network
version: '3.8'

services:
  test-auto-sub:
    image: nginx:alpine
    container_name: test-auto-sub
    ports:
      - "9002:80"
    networks:
      - platform_network

networks:
  platform_network:
    external: true
    name: platform_network
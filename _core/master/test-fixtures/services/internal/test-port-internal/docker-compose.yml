version: '3.8'

services:
  test-port-internal:
    image: nginx:alpine
    container_name: test-port-internal
    ports:
      - "9005:80"
    networks:
      - platform_network

networks:
  platform_network:
    external: true
    name: platform_network